import { describe, it } from '@jest/globals';

describe('LinkedList', () => {
    it.todo('Adds a new node to the beginning of a list')
    it.todo('Adds a new node to the end of a list')
    it.todo('Removes a node from the beginning of a list')
    it.todo('Removes a node from the end of a list')
});